# 竞技场 · 特殊标注映射记录（来自 record\20260825-130920-21e8ad / ...-134310-d222d2）
> 读取规则：实现功能前，都用 tools\read_annotations.py <会话ID> 完整读取每条特殊标注的 (x,y,w,h)+note，并映射到 pipeline/模板。

## 导航
| 界面 | 按钮/元素 | 标注坐标(1920x1080) | 实现 |
|---|---|---|---|
| 二级页 | 模拟军演 | (391,513,148,55) | 模板 `arena_nav_sim.png` |
| 军演页 | 进入竞技场 | (607,602,305,93) | 模板 `arena_enter.png` |
| 主界面 | 出击 | (1708,910,136,105) | 模板 `anchor_chuji.png` |

## 竞技场页(对手阵容)
| 元素 | 标注坐标 | 实现 |
|---|---|---|
| 刷新次数 现/最高 | (1658,149,91,39) | OCR `ArenaReadRefresh` |
| 刷新按键 | (1797,141,89,61) | 模板 `arena_refresh.png` |
| 剩余挑战次数(黄/白) | (355,514,75,50) | OCR `ArenaReadChallenges` |
| 进攻部署 | (1613,949,253,73) | 仅作为竞技场页确认锚点 `ArenaAttack`，不再作为进入挑战入口 |
| 加的积分(+26/+28) | (1790,221,57,48) | OCR `ArenaReadPoints` |
| 对手战力 | (1481,222,146,43) | OCR `ArenaReadOppPower` |
| 进入挑战 | (1690,304,151,127) | 实测固定点击第一行卡片 `(720,335)`，不再使用旧模板 `arena_challenge.png` |

## 挑战确认页（竞技场挑战 step_000）
| 元素 | 坐标 | 实现 |
|---|---|---|
| 挑战按钮 | (1135,932,246,75) | 固定点击标注中心 `(1258,970)` |

## 查看自己战力 / 结算 / 奖励
| 界面 | 元素 | 坐标 | 实现 |
|---|---|---|---|
| 进攻部署查看页 | 自己战力 | (61,211,157,41) | OCR `ArenaReadOwnPower` |
| 胜利结算 | 胜利/失败标识 | (55,56,478,139) | OCR `ArenaResult`(text=胜利) |
| 奖励弹窗 | 点空白处继续 | 任意空白(取960,700) | 固定点击关闭 |

## 策略/次数（interface.json option）
- 刷取策略：尽量刷取高分(需>=28) / 尽量完成挑战(>=26)。
- 重复挑战方式：自定次数(1-10, 子选项) / 重复挑战直到次数归零。
- 校验：自定次数 剩余模拟次数<目标 → 提示“当前剩余次数小于输入次数，请重新修改”。

## 须知
- 所有坐标均为横屏 1920x1080 显示坐标；控制器已 `display_raw:true` 与之对应。
- 页面可能因版本/布局微调而变化；实跑发现不命中时，用读取工具重新核对坐标并更新。
