# 项目备注与后续约定（LAA）

## 1. 跨页面自适应要求（所有任务通用）
- 无论之前任务把游戏停在哪个界面，当前任务必须先识别所在页面，并从二级菜单或更接近目标的页面**直接接续到本任务所需界面**；禁止无条件返回主界面重走全流程。
- 举例：若上一个任务回到的是**二级菜单**（出击页），竞技场应从二级菜单进入，而不是从主界面重进。
- 已知通用导航键：返回上一级 `(164,60)`，返回主界面 `(328,71)`。后续任务应建立“当前页面 -> 下一目标页面”的最短接续路径；只有连续无法识别页面时才允许使用主界面键兜底。
- 任务交接统一使用`agent/navigation.py`中的共享页面图计算最短页面距离：距离不超过2步时按最短路径直接接续；距离超过2步时点击主界面键，确认已经回到主界面后，再从主界面进入目标任务。以后新增的每个任务必须把自己的页面与切换关系加入共享页面图，不得各自实现互不兼容的交接规则。
- 活跃战斗、周本地图等不能直接安全返回主界面的特殊页面，应先按该功能的退出/结算规则离开，再应用页面距离策略；禁止把其他任务页面按宽松颜色特征误判成战斗地图。
- “到达主界面”的共享判据只使用底部固定导航UI，不使用主界面角色、角色皮肤或角色站位。优先要求“基地”和“任务”两个相邻局部模板同时命中；模板因UI换肤或底栏横向位移失效时，一次OCR读取整条底栏，要求基地/任务/编队/队员/构建/补给站/出击中至少两个完整标签命中。主界面进入时钟待机画面、底部UI被隐藏时，先识别时间/星期区域并点击空白处唤醒，再等待固定导航UI出现。
- 主界面须兼容不同展示UI/皮肤：`agent/navigation.py`统一登记主界面入口的语义名称、1920x1080坐标与识别节点，新增任务必须通过该表引用入口，禁止再次散落硬编码坐标。主界面允许“基地+任务模板”或“基地+任务OCR”任一组双命中；仍禁止使用角色外观。当前已预登记底部导航、左侧勘探指南/周期特训/幸运扭蛋、右侧活动入口及顶部和左下图标，为后续功能扩展保留稳定名称。

## 2. 通用弹窗与动画要求（所有后续任务通用）
- 领取物品页面不以具体物品图片作为识别依据。优先识别稳定的弹窗结构、标题、按钮或固定区域；命中后点击录制标注的安全空白处继续，并确认已经离开奖励页。
- “跳过”按钮可能出现在角色动画、BOSS动画或其他演出中。只要稳定关键区域识别到跳过按钮就立即点击；按钮消失后若再次出现，应再次点击，不限定每场战斗只点击一次。
- 上述奖励页和跳过按钮应作为可复用的通用页面处理规则，新任务不得为每一种物品或每一个角色重复保存整屏模板。

## 3. 页面逻辑整理
- 后续要整理并保存**界面切换逻辑**：记录“哪个界面 + 哪个按钮 → 到哪个界面”。
- 录制程序增强（可选）：在录制管理器里加“**同时记录截图+操作**”，你带我走一遍所有页面，我据录制结果整理成界面地图（nodes/overrides）。

## 4. 内容变更后自动重启 LAA
- 每次修改 LAA 内容（interface.json / resource / agent 等）需要重启才能生效时，**自动重启 LAA(MFAAvalonia) 以显示新内容**。
- 实现：`tools\restart_laa.ps1`（或工具），杀进程+按便携 .NET 运行时重启。改完内容后调用它即可。
