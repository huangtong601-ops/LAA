# LAA 项目文档（交接用）

> 交错战线 自动化脚本，基于 MaaFramework + MaaPracticeBoilerplate。
> 程序名：**洛AA（LAA）**，GUI 用 MFAAvalonia 通用界面。更新：2026-08-25。
> 目标：打开 LAA 进入 MFA 管理器界面，由用户手动开始勾选任务；任务通过后台 ADB 操作 MuMu12 上的游戏。

## 一、运行环境
- **硬性磁盘约束：本项目产生的源码、临时脚本、测试输出、截图、构建目录和依赖/编译缓存必须全部放在 `E:\LAA` 或其项目子目录；禁止在 C 盘留下任何项目工作文件或缓存。**
- 需要临时目录时统一使用 `E:\LAA\.tmp`；验证截图统一放入 `E:\LAA\MaaBoilerplate\verification`。构建时显式设置 `TEMP`、`TMP`、`NUGET_PACKAGES` 等缓存路径到 E 盘。
- 模拟器：MuMu Player 12（实例1 = `MuMu模拟器12`，装游戏），ADB 优先使用稳定序列号 `emulator-5556`；MuMu TCP 端口由 MuMuManager 动态读取（本次为 `127.0.0.1:16416`，可能变化）。
- ADB：`E:\MuMuPlayer-12.0\shell\adb.exe`；GUI 检测到 MuMu 时也可能显示 `nx_main\adb.exe`。
- MuMuManager CLI：`E:\MuMuPlayer-12.0\nx_main\MuMuManager.exe`（`control --vmindex 1 launch` / `app launch --package …`）。
- Python：项目虚拟环境 `E:\LAA\MaaBoilerplate\.venv`（**maafw 5.12.3**，自带全部原生库/MaaAgentBinary）。
- GUI 运行时：.NET 10 便携运行时 `E:\LAA\.dotnet`（非自包含 MFAAvalonia 需要）。
- GUI：`E:\LAA\MaaBoilerplate\gui\MFAAvalonia.exe`（显示名/资源名=洛AA）。
- 启动器：`E:\LAA\LAA.exe`（无弹窗，仅打开 MFA 管理器，不传 `--autostart`，不自动开始任务）。

## 二、目录结构与文件
```
E:\LAA\MaaBoilerplate\
├─ 项目文档.md            ← 本文档
├─ 项目备注.md            ← 项目约定（跨页面自适应/页面逻辑整理/改动后重启LAA）
├─ 竞技场标注映射记录.md  ← 竞技场“特殊标注→实现”映射（ROI/模板）
├─ 启动管理器/启动LAA/GUI 脚本(部分已删除，见 tools\restart_laa.ps1)
├─ assets\
│  ├─ interface.json      # 核心接口：pretask/controller/resource/task/option/agent
│  └─ resource\
│     ├─ pipeline\        # 任务管线(JSON)
│     │   ├─ start_game.json  开始游戏（custom_action=startgame）
│     │   ├─ arena.json       竞技场（custom_action=arena_loop）
│     │   ├─ base/weekly/event.json  占位骨架
│     │   └─ (mfa_layout.json 布局)
│     └─ image\           # 模板图（start_game/arena_*/challenge_start/anchor_*）
├─ agent\                 # 自定义动作(agent)
│  ├─ main.py             # AgentServer 注册 arena_loop / startgame
│  ├─ arena_loop.py       # 竞技场循环（导航/读值/策略/挑战/日志）
│  ├─ startgame_flow.py   # 开始游戏（在游戏内即结束；启动页点开始）
│  └─ my_action/my_reco.py 模板示例
├─ tools\                 # 工具与运行器
│  ├─ ensure_emu.py()     # pretask：连接前确保 MuMu 实例1 运行+打开游戏（处理 offline/缺失）
│  ├─ run_all.py()        # 完整实跑：重置→开始游戏→竞技场（测试用）
│  ├─ arena.py()          # 竞技场运行器（--dry/--repeat/--count）
│  ├─ manager.py/.html    # 录制管理器(127.0.0.1:8123)：录制/标注会话
│  ├─ recorder*.py        # 采集/录制（抓屏帧差+getevent 点击）
│  ├─ read_annotations.py # 读取会话全部特殊标注
│  ├─ restart_laa.ps1     # 重启 LAA(MFAAvalonia)
│  └─ requirements.txt    # json-with-comments
├─ gui\                   # MFAAvalonia 程序目录（resource/interface.json 与 assets 同步）
├─ record\<会话>\         # 录制/标注数据（step_*.png + annotations.json + recording.json）
├─ verification\          # 实机验证截图与测试证据（只存放在 E 盘）
├─ debug\                 # 调试截图/日志
├─ deps\tools\            # 接口/pipeline schema（校验用）
└─ *.md(文档)/.venv/node_modules
```

## 三、已实现功能

### 1. 开始游戏（StartGameTask）
- 动作：`startgame`（agent\startgame_flow.py）。
- pretask 先读取 MFA 当前实例；只有“开始游戏”被勾选时才启动 MuMu 和游戏，其他情况直接跳过。
- 2026-08-28 修复已打开MuMu时无法启动游戏：MFA复用已连接控制器时不会再次执行pretask，因此`StartGameFlow`自身也会检查当前配置的ADB设备；若设备停在MuMu桌面或其他应用，先通过ADB启动`com.megagame.crosscore`，确认游戏包进入前台后再开始识别登录页面。pretask同样只使用MuMu实例1动态返回的TCP地址，不再选择任意在线ADB设备。
- 开始游戏任务只在明确识别到主界面底部固定导航UI后成功。游戏包处于前台、Unity加载页、登录动画以及尚未点击“开始游戏”的页面均不能作为完成依据。实机已从MuMu桌面验证：自动拉起游戏、点击开始游戏、处理签到与广告，最终到主界面后返回成功。
- 2026-08-28 更正主界面判据：不再使用“出击”颜色，也不读取任何角色外观。共享导航只在底部固定的“基地”和“任务”局部模板同时命中时确认主界面，最新失败现场实测分数分别为`0.968`和`0.964`；在当前主界面复测`StartGameTask -> OK`。若主界面进入只显示时间与星期的待机画面，则先点击空白处唤醒，待“基地/任务”等底部UI恢复后再确认完成。
- 2026-08-28 新主界面UI适配：新皮肤下旧“基地/任务”模板实测仍为`0.937/0.946`，在保留模板判据的同时增加“基地+任务”限定区域OCR双判据。新增`pipeline/main_ui.json`及`navigation.MAIN_UI_CONTROLS`，统一登记基地、任务、编队、队员、构建、补给站、出击、勘探指南、周期特训、幸运扭蛋、新途补给、星贸凭证、活动、星辉回溯等文字入口；顶部与左下角图标入口也已登记稳定语义坐标，待对应功能开发时补充页面关系和专用图标模板。竞技场和周本已改为从统一表获取“出击”位置。
- 2026-08-28 竞技场现场修复：失败截图实际停在对手列表，旧列表特征在界面动画期间漏判，随后“进攻部署”点击被吞却仍按部署页读取，OCR只得到“战力：”标签。列表页新增“对手阵容/刷新对手+进攻部署”限定区域双识别；读取己方战力改为以数字成功出现作为进入部署页确认，仍在列表时最多重试点击三次。顶部第一位对手战力ROI扩大，并增加稳定读数次数。
- 2026-08-28 主界面横向布局兼容：最新失败现场的底部导航整体右移，旧模板分数降至`0.258`，窄ROI把“基地”和“任务”串入同一区域。共享主界面兜底改为单次OCR整条底栏，并要求基地/任务/编队/队员/构建/补给站/出击至少两个完整标签命中；比逐个入口OCR更快，并兼容两套底栏位置，仍不读取角色。
- 2026-08-28 竞技场低战力修复：顶部第一位实际战力为`662`，OCR连续稳定读到该值，但旧数值校验错误地要求敌方战力至少`1000`，导致任务中止。敌方有效范围改为`1..999999`，符合“敌方约700时也应正常比较并挑战”的既有规则；己方战力读取仍保留`1000`下限以防标签噪声。
- 2026-08-28 项目图标最终采用`laa-icon-candidate-g`（由candidate E放大人物并使头发、发饰和马尾适度越过画框）。通过 Project Interface V2 顶层`icon`字段接入，源码与运行目录均使用`./icon.png`，不修改MFAAvalonia公共可执行文件。
- 开屏按录制 `20260825-121545-794c23` 的特殊标注执行：开始游戏(963,726) → 公告勾选今天不再提示(1608,943)+关闭(1815,146) → 每日签到(1746,906) → 礼包广告勾选今天不再提示(1024,984)+关闭(1676,220) → 主界面。
- 页面判断只比较标注控件附近的局部颜色和结构，不做全屏完全匹配，不依赖 OCR；未知页面停止任务，不盲点。

### 2. 竞技场（ArenaTask）
- 动作：`arena_loop`（agent\arena_loop.py）。
- 流程（不受当前在哪个界面影响）：
  1. `_ensure_arena`：若已在竞技场→直接挑战；否则按当前所在页导航：启动页→点开始游戏；主界面→出击(anchor_chuji)→二级；二级→模拟军演(ArenaNavSim)；模拟军演页→点中间「进入竞技场」卡(ArenaEnter/兜底759,648)→竞技场。采用“点击→确认下一步元素出现→+0.5s”+兜底点卡。
  2. 读值（OCR，ROI 见 竞技场标注映射记录.md）：对手战力[1470,207]、+积分[1693,211]、刷新[1649,129]、模拟[275,383]。
  3. 策略（interface option「刷取策略」）：尽量刷取高分(积分>=28) / 尽量完成挑战(>=26)；且 对手战力<自己战力 才挑战、否则刷新（自己战力在挑战确认页读[585,525]，当前测得 None，待校准）。
  4. 重复（option「重复挑战方式」）：Agent 直接读取 MFA 实例中保存的选项索引；自定次数达到 1-10 的目标后立即结束 ArenaTask，不进入下一轮；也可选择重复挑战直到次数归零。自定次数时剩余模拟<目标会提示“当前剩余次数小于输入次数，请重新修改”。
  5. 挑战：进入挑战(ArenaChallenge→确认页)→读自己战力→点「挑战」→战斗→胜负(OCR胜利)→点空白关奖励→回竞技场。
  6. 日志：已挑战次数、剩余模拟次数、对方战力、挑战是否成功。
- 规则：刷新次数归零不再刷新；模拟次数归零不再挑战。

### 3. 周本 / 活动 / 基建
- 仅占位骨架（`weekly.json`/`event.json`/`base.json`），接口任务已列出，逻辑待开发。

## 四、运行方式
- 打开 LAA：双击 `E:\LAA\LAA.exe`（只打开 MFA 管理器，不自动执行任务）。
- 在 LAA 勾选任务（开始游戏/竞技场…）+ 设好「刷取策略」「重复挑战方式」「次数」→ 点“开始任务”。只有勾选“开始游戏”时 pretask 才会拉起模拟器和游戏。
- 测试/单功能运行：
  - `python tools\run_all.py`（完整实跑：重置→开始游戏→竞技场）
  - `python tools\arena.py [--dry] [--repeat 自定次数|重复挑战直到次数归零] [--count N]`
- 录制/标注：启动 `tools\manager.py`（或 `tools\restart_laa.ps1` 之外手动起 manager），浏览器开 `http://127.0.0.1:8123`；读标注用 `python tools\read_annotations.py <会话ID>`。
- 改动后重启 LAA：`powershell -File tools\restart_laa.ps1`。重启前确认没有多个 `MFAAvalonia.exe` / agent 子进程并存。

## 五、关键机制
- **资源接口固定为 V2**：当前随项目打包的 MFA UI 不支持 Interface V3；不得通过修改接口版本规避启动连接，否则会提示“当前版本的UI不支持当前资源”并退出。打开 `LAA.exe` 仅初始化界面的要求应在 MFA 启动逻辑中实现。
- **MFA v2.15.2 启动修补**：`gui\appsettings.json` 保持 `NoAutoStart=True`；`gui\libs\MFAAvalonia.Core.dll` 基于官方标签 `v2.15.2` 修复 `RootView.axaml.cs`，使该开关同时跳过启动时的 ADB 连接检测且不再被自动重置。显式 `--autostart` 和用户点击“开始任务”的执行路径不受影响。
- **MFA 回滚文件**：原始核心库保存在 `gui\libs\MFAAvalonia.Core.dll.v2.15.2-original`。更新 MFA 时必须重新核对上述启动修补，不能直接恢复旧版“打开即连接模拟器”的行为。
- **pretask**（interface.json）：开始任务后、连接控制器前运行 `ensure_emu.py`；脚本先检查“开始游戏”是否被选中，未选中则不启动任何外部程序。
- **开始游戏耗时优化**：pretask 同时检查 `emulator-5556` 与 MuMu 实际 TCP 地址，不再先空等 20 秒；游戏已在前台时立即返回。开始按钮仅在关键区域连续两帧命中后重试，间隔 7 秒、最多 4 次，避免盲点和 30 秒固定等待。
- **竞技场战力读取**：每次竞技场活动只进入一次标注的“进攻部署”页，从 `[61,211,157,41]` 稳定读取并缓存己方战力；每轮从顶部第一位标注 ROI `[1470,207,168,82]` 多帧读取对手战力。任一读数失败则停止，禁止盲目挑战；固定坐标进入顶部第一位后直接点击挑战，不在确认页重复 OCR。
- **agent**（interface.json `agent`）：MFA 启动 `agent\main.py`（socket），注册 `arena_loop`/`startgame` 自定义动作；自定义动作里用 `context.run_recognition`(OCR/模板) + `context.tasker.controller.post_click`。
- **display_raw**：控制器 `display_raw:true` → 截图 1920x1080，与录制/标注坐标一致。
- **MuMu extras**：控制器 `adb.config.extras.mumu {enable,index:1,path}` 用 EmulatorExtras 高速截图/输入。
- **更改流程**：改 `assets\interface.json`/pipeline/agent 后 → 必须同步到 `gui\` 实际运行目录 → 用 `LAA.exe` / MFAAvalonia 运行态验证。不能只验证 `tools\*.py`，否则可能出现脚本成功但 GUI 仍执行旧代码。
- **标注优先级**：网页录制截图与 `special: true` 区域为最高优先级实现依据；识别采用关键局部命中即可，模板/OCR只作补充。

## 六、当前状态 / 待办 / 已知问题
- [x] 开始游戏：在游戏内即结束（验证 OK）。
- [x] 竞技场：导航(任意页可进/已在竞技场直接挑战)+读值+按策略挑战（完整实跑成功，挑战成功）。
- [x] 双方战力识别：实机只读验证己方 `11700`、当前顶部第一位 `14717`，均连续两次一致；历史确认页示例值不参与实际判断。
- [ ] 周本/活动/基建：待实现（等你录屏+标注）。
- [ ] 跨页面导航全局化：需你标注「返回上一级/返回主界面」UI。录制管理器已支持“记录截图+操作”，可走一遍页面整理“界面地图”。
- [ ] 挑战确认页的“赛季剩余/奖励领取”等辅助逻辑可按需扩展。

## 八、2026-08-25 竞技场 GUI 分叉问题
- 现象：`tools\arena.py` 实测可点第一行并挑战成功，但用户在 `LAA.exe` 打开的 MFA 管理器里仍会点第三行并卡在确认页。
- 真正原因：系统里同时存在多个 `MFAAvalonia.exe` 实例，其中一个由旧 `LAA.exe` 以 `--autostart` 启动；同时存在多套 agent 子进程，导致 GUI 运行态和脚本验证态分叉。
- 修复要求：`LAA.exe` 不再传 `--autostart`；`gui\appsettings.json` 设置 `NoAutoStart=True`；`gui\config\instances\default.json` 中任务默认勾选全部为 `false`；所有后续功能必须同步并验证 GUI 实际运行目录。

## 七、重要文件（改这些）
- 功能逻辑：`agent\arena_loop.py`、`agent\startgame_flow.py`、`agent\main.py`。
- 任务/选项/界面：`assets\interface.json`（+同步 `gui\interface.json`）。
- 管线节点：`assets\resource\pipeline\*.json`（+同步 `gui\resource\pipeline\`）。
- 模板：`assets\resource\image\*`（+同步 `gui\resource\image\`）。
- ROBUST 说明：MaaFramework 不渲染顶层 `input` 选项/下拉带搜索框——目前用「用 select 预设(1-10)作为子选项」，仅选“自定次数”时显示。
# 2026-08-27 任务设置与竞技场战力差

- MFA 任务页不再显示“任务说明”卡片，原区域全部并入“任务设置”。
- 竞技场新增“战力差时依然挑战”输入框，只计算敌方高于己方的差值；敌方低于己方时始终满足战力条件。
- 战力差默认值与当前持久值为 `10000`；MFA 实例配置仍是界面显示来源，agent 同时在 `config\arena_settings.json` 保存最近一次实际读取值。重启后若 MFA 输入数据暂时缺失，使用该 E 盘文件恢复，不要求重新输入。
- 判定边界为严格低于：己方 11700、输入 10000 时，敌方战力低于 21700 才挑战，达到或超过 21700 时刷新。
- 输入仅允许大于或等于 0 的整数，当前默认值为 `10000`，并保留上次保存的数据。
- 开始游戏任务不能只凭“交错战线处于前台”判定完成。已明确识别主界面时直接完成；若同时命中竞技场的“对手阵容/刷新对手”和“进攻部署”两个专属区域，则先点击主界面键 `(328, 71)`，确认底部“基地、任务”等主界面 UI 后再完成。页面完成与竞技场检测必须先于公告、签到和广告颜色判断，禁止在游戏内部页面误点弹窗坐标。
- 前台游戏判定不固定依赖单一地址：先读取 MFA 实例当前 `AdbSerial`，再枚举全部在线 `adb devices`，并兼容 MuMu 常见 `16416`、`16417` 与 `emulator-5556` 标识；任一在线设备的 `topResumedActivity/mResumedActivity` 命中游戏包即确认。开始页必须同时命中标注局部模板和“开始游戏”OCR，禁止仅凭约 0.58 的弱模板相似度把竞技场等内部页面误判成开始页。

## 每日签到奖励后续操作

- 每日签到点击后等待奖励弹出，按开屏录制坐标 `(1071, 929)` 点击空白处领取并关闭奖励。
- 随后按同一录制中的左上返回坐标 `(164, 60)` 返回下一页，再继续处理后续开屏内容。
- 两步均使用用户录制的固定坐标，不新增全屏模板识别。

## 周本导航（录制：竞技场-二级菜单-周本）

- 通用返回键坐标为 `(164, 60)`，主界面键坐标为 `(328, 71)`；竞技场、签到和周本共用统一导航定义。
- 周本路径：主界面 → 出击 → 活动探索 → 碎星虚影 → 戈里刻虚影 → BOSS栏向上拖拽 → 虚影宙斯 → 开始战斗。
- 活动探索即使多出或缺少 `NEW` 标识也按固定标注区域点击，不依赖完整模板。
- 进入戈里刻 BOSS 页后，在 BOSS 栏从 `(950, 850)` 向上拖到 `(950, 300)`，使下方的虚影宙斯进入第三行。
- 点击虚影宙斯后仅检测其第三行大区域边缘的黄色选中框；确认后才允许点击开始战斗。
- 当前流程在进入战斗准备页面后结束；编队、正式开战和重复刷取等待后续录制补全。

## 周本战斗地图（录制：周本操作1.0）

- 战斗准备页默认只使用第一队。如页面出现第二队，点击用户在图3标注的第二队关闭区域，再点击图3右下角“开始行动”。
- 开始后图7至图11属于地图预览动画，不允许提前下达移动指令；只有进入图12所示的正常地图 UI 后才开始判断和移动。
- 正式移动前先拖动画面查看左上区域。情况由两个维度组成：人物出生点为拐角上方的出生点1或拐角右方的出生点2；左上怪物为怪1或怪3，共四种情况。
- “周本操作1.0”图17已标注怪1的关键外观；“周本操作2”图13已补充怪3的完整特殊标注。识别只比较各自稳定关键部位，不要求整幅画面完全一致。
- 情况1定义为“出生点1 + 左上角为怪1”。本次图21至图25是人物第一轮两步，图26至图29是怪物回合，图30至图31是人物第二轮两步并接触 BOSS。
- 地图机制为人物移动两步、怪物移动、人物再移动两步循环。目标是绕开普通怪，只与 BOSS 接触并触发战斗。
- 战斗内检测右上角“跳过”键的稳定关键区域；出现后立即点击。该规则同时适用于周本和竞技场，不依赖完整动画画面匹配。
- 胜利后，图49出现时点击空白区域跳过加载动画进入图51；再点击一次通过结算并返回图53的 BOSS 选择页，以便按任务次数重复。
- 四种情况均已录制完成。怪3不使用紫色发光核心作为特征，只通过其大盾关键部位识别；检测到大盾为怪3，未检测到大盾为怪1。

### 情况2（录制：周本操作2）

- 情况2定义为“人物出生点1 + 左上角为怪3”。本次共保存 57 张截图和 14 条操作，操作录制功能正常。
- 地图进入可操作状态时，镜头默认位于地图最左下角。先在该默认视角判断人物位于出生点1还是出生点2，不先移动人物。
- 判断出生点后，把地图内容向右下拖动以观察地图最左上角。图13是怪物识别视角；关键拖拽为 `(750,224) -> (1204,717)`、约 `994ms`。图13特殊标注区域 `[760,302,223,341]` 为怪3的关键外观。
- 观察阶段还记录了 `(1225,721) -> (1442,447)` 的辅助镜头调整。观察完成后，用 `(1245,803) -> (1121,168)`、约 `2106ms` 调整到执行路线所需视角。三次拖拽均为镜头操作，不计入人物行动步数。
- 情况2的人物路线位于图16至图36，有效移动点击依次为：`(815,504)`、`(812,587)`、`(809,390)`、`(936,249)`、`(1036,324)`。每次点击后等待人物或怪物回合画面稳定，再执行下一步；不要按固定短延时连续点击。
- 第五次移动接触 BOSS，战斗结果显示地图行动步数为 5。战斗动画中“跳过”点击坐标为 `(1735,94)`。
- 图50至图51属于周本奖励弹出，与竞技场、每日签到奖励使用同一处理规则：见到稳定奖励页后点击空白处。本次记录空白点击为 `(1211,908)`；随后胜利结算继续点击为 `(1289,659)`，返回 BOSS 选择页。
- 自动识别时，先判定出生点，再移动镜头判定左上怪物。怪1采用“周本操作1.0”图17特征，怪3采用“周本操作2”图13特殊标注；只比较稳定关键部位，不比较整个动态地图。

### 情况4（录制：周本操作3）

- 情况4定义为“人物出生点2 + 左上角为怪3”。本次保存 47 张截图和 12 条操作；没有新增标注文件，怪3识别继续使用“周本操作2”图13的特殊标注。
- 默认最左下视角中，人物位于拐角右侧区域，可据此判定出生点2。人物未移动前，将地图内容向右下拖动 `(731,407) -> (1443,963)`、约 `2139ms`，观察最左上角并确认怪3。
- 确认怪3后，用 `(1320,919) -> (1132,385)`、约 `904ms` 调整到路线执行视角。两次拖拽均为镜头操作，不计入人物行动步数。
- 情况4有效移动点击依次为：`(1113,561)`、`(1102,447)`、`(949,226)`、`(1043,304)`。每次点击后等待对应人物移动或怪物回合稳定；第四次移动接触 BOSS，战斗结果显示地图行动步数为 4。
- 战斗动画“跳过”点击为 `(1743,67)`。奖励弹窗空白点击为 `(1440,805)`，胜利结算继续点击为 `(1328,691)`，随后返回 BOSS 选择页。
- 自动判定情况4的顺序固定为：默认视角确认出生点2 -> 移动镜头 -> 在左上怪物区域匹配怪3关键特征 -> 执行情况4四步路线。不得仅凭出生点2直接选择路线，因为情况3同样使用出生点2。

### 情况1与情况3补录

- 情况3来自 `record/20260827-130815-89dd8f`：出生点2 + 左上角怪1。镜头操作依次为 `(714,337)->(1168,779)`、`(1237,784)->(1323,782)`、`(1155,865)->(841,285)`；移动路线依次为 `(1110,551)`、`(1074,623)`、`(1040,571)`、`(1123,451)`、`(968,229)`、`(1019,324)`。
- 情况1重新补录于 `record/20260827-131256-adf4c5`：出生点1 + 左上角怪1。镜头操作为 `(560,535)->(1121,948)`、`(859,701)->(846,499)`；拖拽终点附近的 `(1110,938)` 未造成角色移动，不计入路线。有效移动路线依次为 `(828,472)`、`(864,398)`、`(1129,291)`、`(1076,304)`、`(1048,317)`。
- 自动识别顺序为：地图正常操作界面出现 -> 默认视角通过角色头顶`01`标记识别出生点 -> 使用该出生点对应的录制镜头朝BOSS方向观察左上怪物 -> 仅匹配怪3大盾关键部位。命中大盾后接着执行怪3情况的剩余镜头与路线；未命中则继续执行怪1情况中同样朝BOSS方向的录制镜头，禁止反向拖回主角方向，也禁止使用紫色发光核心分型。
- 若本周奖励已满，点击开始战斗后会出现“本次挑战将不获得奖励”的确认弹窗；周本任务识别该文字后点击右侧“开启”继续，不得把后续的关闭第二队与开始行动点击落在弹窗上。
- 地图移动严格按“人物两步 -> 等待怪物回合结束 -> 人物两步”同步：单步间等待约 2.4 秒；每个偶数步后只识别右下角“我方回合”四字，等待其先消失、再重新出现，确认怪物回合完整结束后才点击下一步，识别异常时兜底等待满 10 秒。该公共逻辑应用于情况1/2/3/4。结算只匹配左上角“战斗胜利”的局部模板，不再使用会在地图内误命中的宽松 OCR。
- 2026-08-27 情况2实际运行日志确认：第1至第4步生效，第5步在第4步后约 6.05 秒点击，而录制间隔约 6.95 秒，导致点击仍处于怪物回合并被吞掉；已用上述回合状态同步替换固定 6 秒等待，修复方式与情况4一致并应用于四种情况。
- 2026-08-27 实机试跑情况4成功：从主界面进入碎星虚影/戈里刻虚影/虚影宙斯，处理奖励上限提示，关闭第二队并开始行动；识别出生点2与大盾后判定情况4，按录制路线触发 BOSS，识别并点击跳过键，完成结算后实际返回虚影宙斯 BOSS 选择页。验证截图位于 `verification/weekly_success.png`。
- 2026-08-27 修复情况1误判为情况2：误判帧的大盾模板分数仅 `0.562663`，旧阈值 `0.52` 使动态画面中的局部相似区域误命中；历史真实怪3大盾分数为 `0.998089`。大盾局部模板阈值提高至 `0.80`，并改为最多检查三帧、至少两帧命中才判为怪3；单帧动画或残影命中统一按怪1处理。
- 2026-08-27 周本入口改为页面状态接续：每次执行先辨认主界面、二级菜单、活动选择、周本选择、BOSS选择或战斗准备页，只点击从当前页面到后续页面所需的步骤；连续刷取返回BOSS选择页后直接复用当前虚影宙斯选择，不再退回主界面重走。只有连续两次无法辨认页面时才允许使用一次主界面键兜底。
- 2026-08-28 修复竞技场到周本交接：周本旧地图的宽松颜色判断曾把竞技场列表误判成周本地图，导致错误执行返回键与撤退确认。页面检测现优先识别竞技场刷新按钮和竞技场文字，再进行地图颜色判断。共享导航新增页面图与两步阈值：竞技场列表到周本活动选择入口最短距离为3，因此直接点击主界面键；模拟军演页到周本入口为2，可以返回二级菜单后接续。实机已从竞技场列表完整验证到周本BOSS选择页，未进入战斗。
- 2026-08-27 更正周本镜头方向：四种情况的录制拖拽均只朝BOSS区域推进，不存在返回主角方向的镜头操作。删除反向及1.8倍反向恢复逻辑；怪物分型完成后，怪3接着执行已完成探测后的剩余录制镜头，怪1继续重放其本情况的全部BOSS方向镜头，利用地图边界归一到该路线的录制视角后再点击移动格。
- 2026-08-27 出生点识别不再依赖固定挑战人物模型：优先OCR角色头顶固定的`01`队伍标记，根据标记横坐标区分出生点1/2；旧人物局部模板仅作为OCR失败时的兼容兜底。因此更换出战角色后仍可判定出生位置。
- 2026-08-28 日志确认一次BOSS前卡住属于情况2：出生点1、怪3大盾分数`0.983/0.993`，前四步及回合切换成功，但人物移动时相机会自动跟随，使BOSS标识移到约`Rect(901,0,65,22)`，此时直接使用录制第五点会失效。情况2第四步后先识别BOSS，再执行两次`(960,120)->(960,900)`竖直向下长拖到地图最上角；实机确认此时BOSS标识稳定在约`Rect(1004,88,62,36)`，复核后点击固定接敌点`(1036,324)`。删除了会把BOSS拖出画面的横向补正拖拽。
- 2026-08-28 已在原卡住现场确认`BOSS`可能被OCR为`8055`，对应识别框`Rect(x=900,y=0,w=67,h=24)`，兼容项已加入。补正识别最多尝试两轮，识别到战斗跳过键后本轮永久停止地图补正，避免战斗特效造成重复日志或误操作。
- 2026-08-28 情况1失败现场的行动次数保持`0/99`，确认五个固定移动点从首点开始均未生效，根因是怪物分型观察镜头与执行镜头叠加后没有回到固定视角。实机确认无论当前镜头位置如何，从`(960,900)`竖直向上长拖到`(960,120)`两次即可抵达地图左下角，静态金色障碍物锚点稳定为约`(686,124)`，且不会增加行动次数。四种情况完成分型后统一执行该左下角归一，静态锚点确认偏差不超过24像素后才使用各情况原录制固定坐标；不再重放各情况额外镜头，也不再按人物或视角动态平移点击。角色头顶`01`只作为静态锚点被遮挡时的辅助，人物外观不参与路线校准。根据后续要求，不再逐步OCR行动次数：同一人物回合内两步之间固定等待`4.2`秒，每两步后的怪物回合固定等待`8.5`秒，末步等待`2.0`秒，以降低识别开销并避免动画吞指令；情况2实机曾在`2.8`秒时吞掉第四步，故统一放宽。
- 2026-08-28 最终实机验证：情况1、情况2、情况3、情况4均已各自完整执行并击败BOSS，其中情况2由用户现场确认胜利。情况2接近BOSS后执行两次`(960,120)->(960,900)`竖直向下长拖，将视角固定到地图最上角，再点击固定接敌点`(1036,324)`；不再根据卡住画面对照推算临时坐标。若地图阶段固定等待`20`秒仍未离开地图，则等待地图稳定后点击左上返回键与撤退确认，确认已回到BOSS选择页后重新开局并重新判断情况。胜利/奖励识别优先于地图卡住判定，避免结算画面被宽松地图特征抢先误判。
- 2026-08-28 门前拖拽优化：原来的两次操作是把镜头拖到地图最上角，不是点击格子。现在第一次竖直拖拽后立即检查BOSS锚点；若已进入标准范围就省略第二次，仅在第一次仍未到边界时补做第二次。战斗中的跳过按钮改为按“每次重新出现均点击”处理，角色动画与BOSS动画使用同一规则。
- 周本默认刷取次数为 5。任务设置新增右侧打勾样式的单项开关“本周报酬全部领取后结束”，不显示额外的“结束条件”标题，默认勾选：勾选时进入戈里刻 BOSS 页面后读取左下角周报酬进度，检测到 `600/600` 即结束本次任务并跳过队列中的后续重复；取消勾选时忽略 `600/600`，严格按用户设置的刷取次数继续执行。

## 2026-08-27 竞技场连续执行修复

- 战斗后不再固定等待 26 秒；循环检测左上角“战斗胜利”局部关键区域，出现后立即点击继续。
- 新增“获得物品”局部模板，检测到奖励弹窗后点击空白处，并确认回到竞技场对手列表再开始下一轮。
- 2026-08-27 实际卡住截图确认：奖励遮罩后面的刷新次数仍可被 OCR 读到，旧逻辑因此把领取页误判成对手列表。现改为 OCR 识别“获得物品”，并用标题白色区域与深色遮罩作兜底；奖励或胜利遮罩存在时禁止 `_at_arena` 返回成功，必须点击至遮罩消失后才继续。
- 修复后已由 LAA/MFA 实际完整挑战 1 次验证：读取己方战力 `11700`、顶部对手战力 `14717`，点击顶部第一位并完成战斗；识别胜利页后进入“获得物品”，自动点击至遮罩消失，确认回到竞技场列表，日志最终状态为 `SUCCEEDED`。返回列表截图位于 `verification/arena_reward_fixed.png`。
- 最新规则：刷新次数为 0 时仍判断当前顶部第一位；若其战力与积分条件均符合则正常挑战，只有当前第一位不符合且无法继续刷新时才结束，并记录“刷新次数归零，剩余挑战次数（X）次”。计数器按 `当前值/总值` 读取左侧值，禁止再把 `0/15` 误读成 15、把 `5/10` 误读成 10。
- MaaFramework Python API 的 `tasker.stopping`/`tasker.running` 是属性。所有自定义任务统一在截图、点击、滑动和循环入口检查“正在停止或已停止”；用户点击停止后不再产生后续输入。
- 竞技场等待、识别、刷新与点击路径均检查 MFA 停止状态；用户点击“停止任务”后不再继续刷新。
- 2026-08-29 日志确认开始游戏在竞技场列表把右上区域误判为公告，连续点击公告关闭坐标 `(1815,146)`，实际落在刷新区域附近。开始游戏现先用竞技场标题与“进攻部署”双特征确认内部页面，只点击主界面键并等待主界面，不再执行公告/广告点击。
- 竞技场结束决策收敛为单一顺序，适用于所有刷取策略：模拟次数连续两次识别为 `0` 时结束；模拟次数不为 `0` 且当前候选满足战力/积分条件时挑战，即使刷新次数为 `0` 也必须挑战；候选不满足且刷新次数大于 `0` 时刷新；候选不满足且刷新次数连续两次为 `0` 时结束，并报告“刷新次数归零，当前对手不符合挑战要求，剩余挑战次数（X）次”。模拟次数无法识别时禁止刷新、挑战或结束；刷新次数无法识别且候选不合格时同样只重试读取。
- 2026-08-29 修复结算后停在挑战准备页却结束任务：竞技场次数仅允许在明确识别到对手列表后读取，不能再以单个次数数字作为页面依据；挑战按钮被吞时最多重试三次；只有识别到“战斗胜利”或奖励页并确认返回对手列表，才把本轮计为完成。结算空白点击若误打开对手准备页，会先返回列表再继续。
- “重复挑战直到次数归零”不读取也不受“自定次数”目标限制。对手战力暂时无法稳定识别时，如仍有刷新次数则刷新并重新读取，不再直接提前结束；模拟和刷新计数器的单次 `0` OCR 均不得直接触发结束。

## 芯片筛选

### 页面与读取逻辑

- 录制依据：`record\20260829-140845-aa1961`（芯片筛选1.0）与 `record\20260829-142419-43294c`（芯片筛选2.0）。截图特殊标注的位置优先于重新推测的坐标。
- 路径：主界面 ↔ 仓库 ↔ 芯片区。主界面仓库按钮为 `(1723,63)`，仓库右上芯片页签为 `(1800,70)`。任务可从已知中间页面接续；正式扫描前会返回主界面再重进仓库，因为游戏会记忆仓库滚动位置，重新进入才会可靠回到第一枚芯片。
- 芯片每行 6 枚。任务从左到右、从上到下扫描仓库容量所表示的全部已有芯片；每屏读取 3 行共 18 个完整栏位，不点击底部被裁切的第 4 行。翻页通过右侧滚动条每次精确下移三行，并使用翻页后校准坐标，避免网格拖拽惯性造成重复或漏读。详细页读取后点击 `(300,700)` 的页外空白处关闭。
- 详细页第 1 行黄色词条是主技能，第 2、3、4 行是 3 个副技能；每行右侧等级只允许 `1/2/3`。四行完整合法且连续两次相同才接受，读取失败只记录并跳过，禁止盲目上锁。
- 详细页会随被点击芯片所在行上下偏移，“上锁/弃置键”不能使用固定纵坐标。程序根据详情第一条技能的实际纵坐标动态计算按钮位置，切换后关闭详情，并通过芯片卡左上角的锁标记二次确认；复核失败时禁止重复盲点。
- 当前 LAA 任务名为“芯片筛选-仓库”。任务根据 `config\chip_filter_plan.json` 对仓库全部芯片做双向纠正：未锁且符合方案的上锁，已锁但不符合方案的取消上锁，状态已正确的保持不变。结果写入 `config\chip_scan_latest.json`，包含读取、上锁、解锁、未变更、读取失败和复核失败统计，仅使用 E 盘项目目录。
- 读取器是独立 agent 模块，详情识别与方案判定后续可从强化、编队等其他芯片列表复用；仓库全量遍历仅属于“芯片筛选-仓库”任务。用户停止任务时在截图、点击、滑动和循环入口立即终止，不再产生后续操作。

### 技能分类

- 主技能：穿甲、切割、征服、重击、支援、精力、蓄能、收割、屏障、铁壁、灵巧、暴怒、致命、腐蚀、集中、金刚、痛击、扩大、物攻、能量、装填、光幕、钝化、特防、神威、神力、神速、振奋、消除、重伤、连击、乘风、反击、协击、引爆。
- 副技能：攻击、耐久、防御、速度、瞄准、暴伤、命中、坚韧。

### 自定义筛选方案早期构想（历史记录）

- 本节保留最初讨论过程，当前实际行为与界面以后一节“自定义筛选方案页面第一版”和“页面与读取逻辑”为准；其中“只上锁、不解锁”等早期设想已经废止。

- 参考《鸣潮》声骸管理的“方案列表、独立启用、组合条件、检查已有库存”组织方式，但不照搬它的“自动上锁/自动弃置”双状态配置。LAA 方案统一命名为“自定义需要上锁的方案”。
- 芯片只产生两种最终判定：`需要上锁` 与 `不需要上锁`。仓库筛选任务会把每枚芯片的实际锁定状态纠正为方案结果；不标记弃置、不分解、不强化。
- 顶部是方案列表，每个方案带真实勾选框“启用”，支持新建、重命名、复制和删除。多个启用方案之间为 `OR`：任意一个方案匹配即上锁。方案按名称持久保存，重启 LAA 不丢失。
- 每个方案内是多个“主技能规则”，规则之间也为 `OR`。每条规则先选择 1 个主技能与最低等级 `1/2/3`，然后展开该主技能专属的副技能要求。不设全局统一副技能，保证不同主技能可使用不同的配套。
- 副技能要求区列出 8 种副技能；勾选需要的副技能后，为其设置最低等级 `1/2/3`，再设置“至少满足 N 项”。单条规则的逻辑是：主技能名称与等级合格 `AND` 副技能条件达到 N 项。例如“切割≥2，命中≥2、攻击≥2、暴伤≥1 中至少满足 2 项”。
- 执行区只有“仅预览”和“按方案上锁”两种模式，不提供弃置模式。首次使用新方案时必须先预览，显示扫描数、匹配数与每枚芯片的命中规则，再由用户执行上锁。
- 底部仅保留必要勾选框：“已上锁芯片跳过”、“读取失败时停止任务”。
- 每个单独方案都可生成可复制的方案码，格式前缀为 `LAA-CF1-`。不同用户导入同一方案码时，得到同名且规则完全相同的筛选方案。方案码是离线的规则序列化结果，不需要服务器。
- 方案码只包含方案名、主技能、副技能、最低等级与“至少满足 N 项”。不包含用户名、设备号、文件路径、本地启用状态和执行上锁开关。导入后只新增/覆盖规则内容，仍需用户在本地启用并先预览。
- 方案码使用规范化 JSON、压缩、URL-safe Base64、版本前缀和校验值生成；相同语义的规则即使勾选顺序不同也会产生相同方案码，篡改或损坏的方案码必须拒绝导入。版本前缀用于以后兼容规则升级。

### 自定义筛选方案页面第一版

- 2026-08-29 已在 LAA 使用的 MFAAvalonia v2.15.2 内加入专用页面。任务说明区域默认缩小；仅对需要芯片方案的任务在说明区显示“点击自定义芯片筛选方案”按钮，点击后在同一 LAA 进程内打开与主窗口工作区同尺寸的方案窗口，不使用网页或独立脚本界面。
- 页面顶部提供方案名称和方案码导入框；中间横排显示主词条 `1/2/3` 级，每级使用“锁定 / 条件锁定 / 不锁定”三态分段控件。默认依次为“不锁定 / 条件锁定 / 锁定”。
- “锁定 / 条件锁定 / 不锁定”三个选项无论是否选中都始终显示文字，当前选项使用蓝色强调。选择“条件锁定”后显示该等级下全部 35 种主词条及 `已配置/待配置` 状态；当前词条显示为整块绿色按钮。单击主词条只切换当前词条，双击才进入三级副词条向导，依次设置“副词条 >=1 / >=2 / >=3 时锁定”。每级包含 8 种副词条和“无”；选择“无”会清空本级其他项，同一副词条只允许归入一个最低等级。
- “复制当前配置”只列出当前等级尚未配置的主词条，可勾选部分目标后复制，不再强制复制到全部未配置词条。其右侧提供红色“清空当前配置”按钮，二次确认后只清空当前正在查看的主词条等级的全部条件锁定配置，不影响另两个等级。
- 所有处于“条件锁定”的主词条等级必须完成全部 35 类配置后才能保存。保存成功询问是否导出方案码；方案码窗口允许文本选择、按钮复制，并显示 10 秒“已复制到剪贴板，快去分享吧~”提示。
- 第一版方案码升级为 `LAA-CF2-`，包含三级主词条模式及各主词条的三级副词条映射；带 SHA-256 截断校验、32 KiB 输入上限和 64 KiB 解压上限。方案保存到 `config\chip_filter_plan.json`。
- 默认方案的主词条 2 级使用读图整理出的 35 种主技能推荐表：任一推荐副技能达到 2 级及以上即锁定；主词条 1 级默认不锁定，3 级默认直接锁定。攻略图中的“爆伤”按游戏内正式词条名“暴伤”保存；切割除攻击、暴伤、瞄准外，按原图补充说明同时接受命中。
- 页面额外提供配置进度；复制目标列表只包含尚未配置项，因此不会覆盖已经逐项设置的类别。
- 可复现的 MFAAvalonia 定制补丁和 E 盘构建脚本位于 `ui_custom\MFAAvalonia`，目标上游提交固定为 `6065fe33798b72906c5079fa6f210646801d9a5c`。

## 操作录制器 v2

- 抓屏轮询间隔由约 `350ms` 提高到约 `180ms`，并降低 PNG 压缩级别以缩短保存耗时。
- 每次点击、长按或滑动结束后，分别在约 `100ms` 和 `650ms` 安排候选截图，再按画面差异去重，兼顾快速操作与动画结束后的页面。
- `recording.json` 新增 `operations` 时间线；一次完整触摸只生成一个 `tap`、`long_press` 或 `swipe`，滑动记录起点、终点、耗时及简化轨迹。
- MuMu 可能把鼠标操作上报为多点触控，也可能上报为鼠标整合事件；录制器同时监听 `ABS_MT_* / BTN_TOUCH` 和 `ABS_X/Y / BTN_LEFT`，避免出现截图完整但操作时间线为空。
- 2026-08-27 已查明“周本操作1.0”操作数为 0 的原因：MuMu 的 `getevent -lt` 会把按钮状态输出为文本 `DOWN/UP`，旧解析器按十六进制数转换后静默跳过；同时结束后的 `SYN_REPORT` 可能造成重复操作。现已兼容文本按钮值，并要求只有活动触摸状态才能采样同步帧。
- 修复后已完成三层验证：直接点击只生成一条 `tap`；拖拽只生成一条 `swipe` 且含起点、终点和轨迹；通过录制网站 API 开始/停止后，实时状态和最终 `recording.json` 均保留同一条操作。
- 录制网站实时显示操作时间线。停止录制后可多选“忽略所选”或“恢复所选”；忽略为可恢复标记，不物理删除原始操作。
- 后续读取录制时优先采用 `operations` 中 `enabled != false` 的操作；兼容字段 `taps` 会自动排除已忽略操作。
- 缩略图保持完整画面比例，不裁剪；图片列表不再使用独立高度限制，统一由左侧栏纵向滚动。
- 鼠标悬浮缩略图时右上角显示删除按钮；支持 `Ctrl+点击` 多选和按住鼠标划过多张图片批量选择，点击任一已选图片的删除按钮可删除整组。
- 图片删除同样只设置 `recording.json` 中的 `enabled=false`，不删除 PNG 或标注。`Ctrl+Z` 可按删除批次连续撤回；网页重开后可在“已删除图片”区域恢复。
- 后续读取录制截图时，只使用 `screens` 中 `enabled != false` 的图片。
- 每张缩略图左上角显示稳定编号：`step_000.png` 对应“图1”，编号按原始文件名确定，删除或恢复后不重新排列，便于用户准确引用。
- 已保存的特殊标注支持再次编辑：点击大图中的黄色标注区域，或点击下方对应的坐标/说明条目，会打开带原说明的修改框；确认后自动保存。删除按钮仍只负责删除标注，不触发编辑。

## 基建功能录制理解（会话：主界面-基地等收菜操作）

- 录制来源为 `record/20260828-175606-4cc346`，共记录约 76 幅有效过程图和 41 条操作。下述内容先作为页面地图与实现依据；订单取舍策略仍需用户确认后再固化为任务逻辑。
- 页面主链：主界面 --点击底部“基地” `(947,988)`--> 基地总览。基地总览可通过左上返回键回到上一页，也可通过左上主界面键直接回主界面；后续基建任务需要从主界面、基地总览、驻员页、订单页或合成页识别当前位置并接续，不得统一先退出重进。
- 基地总览以“行星指挥部、能源发电站、合成工厂、研发中心、宿舍、原料交易所、挖掘矿场”等固定建筑文字和左侧“基地建造/驻员状况”为页面判据，不依赖人物外观。图5（`step_004.png`）标注三个入口：收菜区 `[673,665,309,98]`、换人区 `[143,868,175,50]`、换票区 `[1654,452,89,86]`。
- 收菜流程：基地总览 --点击收菜区--> “获得物品”弹层 --点击空白处--> 基地总览。奖励物品内容可能变化，沿用竞技场、签到的通用奖励弹层处理，不按物品图案做完整匹配。
- 驻员流程：基地总览 --点击左侧“驻员状况/换人区”--> 驻员状态列表 --点击某设施右侧“驻员预设”--> 预设弹窗 --点击备用预设的“使用”--> 驻员状态列表。图13（`step_012.png`）标注绿色笑脸为好心情 `[1001,313,52,50]`，红色难过脸为坏心情 `[996,518,59,57]`；目标是识别坏心情设施并切换到含好心情驻员的备用预设。列表超出首屏时按录制向上滑动继续检查。
- 订单入口：基地总览 --点击原料交易所旁的换票区/订单图标--> 自主订单库。自主订单库顶部显示“10级订单库：N/N”，他人订单库顶部显示“玩家名的N级订单库 N/N”；图63、图64分别标注他人订单库标题 `[591,42,350,73]` 与自主订单库标题 `[596,47,278,73]`，实现时用顶部文字区分，不能只看订单卡片。
- 订单卡需要分别读取奖励类型、奖励等级/数量、消耗素材类型及消耗数量，并只点击状态为“可交付”的黄色按钮。录制标注样例包括：自主订单库中的“构建订单 IV”消耗 18 个稀有黑匣，以及好友订单库中的构建订单消耗 10、稀有技术点订单消耗 10；这些样例用于建立识别区域，不代表最终固定优先级。
- 交付流程：订单库 --点击订单卡底部“交付”--> “是否确认提交订单”弹窗 --点击右侧“确定”--> 返回当前订单库。若按钮显示“资源暂缺”或为灰色则不得点击；交付后库存和订单库数量会即时变化，需要重新截图再决定下一单。
- 好友订单流程：自主订单库 --点击左下“选择好友”--> 好友列表 --点击目标好友“拜访”--> 他人订单库。好友订单库左下左右箭头可切换上/下一个好友；每次切换后必须等顶部玩家名和订单列表稳定，再读取可交付订单。左上返回键返回上一级，主界面键直接退出该分支。
- 订单素材库存固定显示在订单页左侧。图33（`step_032.png`）标注三类基础库存：星币订单素材、技术点订单素材、构建订单素材及各自数量；下单前应把订单卡消耗类型与左侧库存对应，禁止把卡片上方的奖励数量误当作消耗数量。
- 合成入口：订单页 --点击左上“制作素材”--> 素材合成目录。图68（`step_067.png`）标注“星币原料”为星币订单素材、“数据硬盘”为技术点/经验订单素材、“稀有黑匣”为构建订单素材；点击对应卡片进入该素材的合成详情。
- 合成详情页显示驻员、副产品概率、素材配方、当前库存、合成份数和“确定”。图76（`step_075.png`）标注数量控制从左到右为最少 `[1347,509,116,57]`、减一 `[1472,509,114,57]`、加一 `[1602,512,116,55]`、最大 `[1736,510,114,57]`。自动化需要根据订单缺口设定份数，确认原料足够后点击“确定”，再返回订单页继续交付，不能无条件使用“最大”。
- 当前理解下的完整工作闭环为：识别当前页面并接续 -> 收取基地可领取产出 -> 检查各设施驻员心情并轮换坏心情队伍 -> 读取自主订单 -> 在可交付订单中按后续确认的策略交付 -> 必要时浏览好友订单 -> 缺少目标订单素材时进入合成目录按缺口生产 -> 返回订单库继续交付 -> 完成设定目标后结束任务或回到主界面交接下一任务。
- 待用户确认的策略项：订单奖励优先级、是否接受普通素材/高级素材订单、是否优先好友低消耗订单、每日希望交付的数量上限、合成时优先选择哪组驻员及是否允许消耗全部基础原料。在这些规则确认前，只实现页面识别与操作骨架，不自行提交订单或批量合成。

## 基建-订单库（BaseExchangeTask）

- 原任务“基建·去他家兑换物资”统一更名为“基建-订单库”。实现入口为 `agent/base_order_flow.py` 的自定义动作 `base_order_flow`，实际运行资源同步到 `gui` 后由 LAA/MFA 调用。
- 设置“默认兑换构建票订单数额”按紧凑方框布局显示：标题下第一行是独立的“缺少素材时自动合成”，第二行是 `6/8/10`，第三行是 `16/18`；数额默认勾选 `6/8/16`，自动合成默认不勾选。另有“兑换稀有星币订单”“兑换稀有技术点订单”两个一级方框，默认不勾选；一级方框打勾后才显示各自同为方框样式的“缺少素材时自动合成”二级项。订单库任务不使用 Yes/No 颜色选择控件或胶囊式多选按钮。
- 导航遵循全项目页面接续规则：已在自主订单库、好友订单库、好友列表或制作素材页面时按最短路径接续；在主界面进入基地总览并点击原料交易所换票区；未知跨任务页面使用主界面键后重新进入。
- 执行顺序固定为：先反复扫描并交付自主订单库中所有符合设置的订单；然后点击“选择好友”，从第一位好友开始逐个扫描，通过左下“下一位”箭头切换；顶部标题重新变为自主订单库时表示好友已全部遍历并结束任务。
- 自主与好友订单库都按固定 `4×2` 八个栏位分别 OCR 奖励类型、稀有标识、消耗数量与交付状态。好友等级不足时可能少于八个有效栏位；空白、未解锁或没有识别出订单类型的栏位直接略过，不报错也不阻止处理其余栏位。构建票订单只按用户勾选的稀有黑匣消耗数额决定；星币与技术点订单只有卡片左上命中“稀有”标识且对应一级开关开启时才处理。
- 自动合成仅在目标订单明确显示“资源暂缺/素材不足”时进入，先连续读取订单页左侧对应素材库存，再计算 `缺口=订单消耗-当前库存`。合成详情先点“最少”，再按缺口加一，只有连续 OCR 确认合成份数与缺口完全一致时才点“确定”；合成后返回原来的自主或好友订单库继续交付。若合成所需基础原料不足、数量无法确认或页面返回失败，本单跳过并记录日志，禁止盲目消耗。
- 好友订单的当日共享限制按“订单类型 + 普通/稀有 + 消耗数量”识别。例如任一好友处交付过一次“普通构建订单-18”，当天后续好友均跳过同类 `18` 订单；自主订单库不受该记录影响。记录保存在 E 盘项目内 `config/base_order_daily.json`，日期变化时自动清空。
- 所有截图、点击、等待、合成加减与好友切换循环都调用统一停止保护。用户在 MFA 点击“停止任务”后不得再产生输入；任何不可恢复错误返回失败，并由 `ContinueRunningWhenError=false` 阻止后续任务继续执行。
